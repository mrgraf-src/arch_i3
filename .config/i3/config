# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

bindsym $mod+b focus_follows_mouse yes
bindsym $mod+n exec --no-startup-id i3-msg 'focus_follows_mouse no'


# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans regular 10
#font pango:Ubuntu Regular 10
font pango:awesome regular 10




# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id rofi -font "hack 10" -show drun

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle


# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+z floating toggle

# variables for left and right monitor 
# (assuming names DP-1 and DP-3 with DP-1 being left of DP-3)
set $monL HDMI-0
set $monC DVI-D-0
set $monR DP-1


# variables for workpacenames; 
set $ws1L "  "
set $ws1C "  "
set $ws1R "  "

set $ws2L "  "
set $ws2C "  "
set $ws2R "  "

set $ws3L "  "
set $ws3C "  "
set $ws3R "  "

set $ws4L "  "
set $ws4C "  " 
set $ws4R "  "

# assign workspaces to specific outputs
workspace $ws1L output $monL
workspace $ws1C output $monC
workspace $ws1R output $monR

workspace $ws2L output $monL
workspace $ws2C output $monC
workspace $ws2R output $monR

workspace $ws3L output $monL
workspace $ws3C output $monC
workspace $ws3R output $monR

workspace $ws4L output $monL
workspace $ws4C output $monC
workspace $ws4R output $monR

workspace $ws5L output $monL
workspace $ws5C output $monC
workspace $ws5R output $monR

workspace $ws6L output $monL
workspace $ws6C output $monC
workspace $ws6R output $monR

workspace $ws6L output $monL
workspace $ws6C output $monC
workspace $ws6R output $monR

# do not toggle automatically between the current and previous 
# workspace on repeated calls to `workspace NAME` (with identical NAME)
workspace_auto_back_and_forth no

# Toggle between both monitors 
# (focus output left wraps from the leftmost to the rightmost output)
bindsym $mod+Tab focus output left
bindsym $mod+Shift+Tab focus output right

# switch two workspaces at once (the need to be on different outputs)
bindsym $mod+1 workspace $ws1C
bindsym $mod+2 workspace $ws2C
bindsym $mod+3 workspace $ws3C
bindsym $mod+4 workspace $ws4C
bindsym $mod+5 workspace $ws5C
bindsym $mod+6 workspace $ws6C

# bindsym $mod+1 workspace $ws1C, workspace $ws1R, workspace $ws1L

# move a window to a specific workpace; 
# $mod+Shift for left side, $mod+Control for right side

# move a window to a specific workpace; 
# $mod+Shift for left side, $mod+Control for right side
bindsym $mod+Control+1 move workspace $ws1L
bindsym $mod+Shift+1 move workspace $ws1C
bindsym $mod+Control+Shift+1 move workspace $ws1R

bindsym $mod+Control+2 move workspace $ws2L
bindsym $mod+Shift+2 move workspace $ws2C
bindsym $mod+Control+Shift+2 move workspace $ws2R

bindsym $mod+Control+3 move workspace $ws3L
bindsym $mod+Shift+3 move workspace $ws3C
bindsym $mod+Control+Shift+3 move workspace $ws3R

bindsym $mod+Control+4 move workspace $ws4L
bindsym $mod+Shift+4 move workspace $ws4C
bindsym $mod+Control+Shift+4 move workspace $ws4R

bindsym $mod+Control+5 move workspace $ws5L
bindsym $mod+Shift+5 move workspace $ws5C
bindsym $mod+Control+Shift+5 move workspace $ws5R

bindsym $mod+Control+6 move workspace $ws6L
bindsym $mod+Shift+6 move workspace $ws6C
bindsym $mod+Control+Shift+6 move workspace $ws6R


# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

#bindsym $mod+Shift+e exec xfce4-session-logout
bindsym $mod+Shift+e exec /home/mrgraf/.config/polybar/scripts/powermenu.sh

bindsym $mod+r mode "resize"



#Добавляет title всем окнам, включая стим., срань, не подходит..
#for_window [all] border pixel 0, border normal 0

for_window [all] title_window_icon yes
for_window [all] title_window_icon padding 5px

#Убирает заголовки всем окнам
#, border normal 0
for_window [class="^.*"] border pixel 0



#for_window [class="TelegramDesktop"] border pixel 0, border normal 0
#for_window [class="Sublime_text"] border pixel 0, border normal 0
#for_window [class="Audacious"] border pixel 0#, border normal 0 
#for_window [class="KeePassXC"] border pixel 0, border normal 0
#for_window [class="flameshot"] border pixel 0, border normal 0
#for_window [class="Thunar"] border pixel 0, border normal 0
#for_window [class="Pcmanfm"] border pixel 0, border normal 0
#for_window [class="kitty"] border pixel 0, border normal 0
#for_window [class="obs"] border pixel 0#, border normal 0

#for_window [class="com-atlauncher-App"] border pixel 0, border normal 0
#for_window [class="Indicator.py"] border pixel 0, border normal 0

#for_window [class="Microsoft-edge"] border pixel 0#, border normal 0
#for_window [class="YandexMusic"] border pixel 0#, border normal 0
#for_window [class="Pavucontrol"] border pixel 0#, border normal 0

#for_window [class="com-atlauncher-App"] border pixel 0#, border normal 0
#for_window [class="Minecraft"] border pixel #0#, border normal 0
for_window [class="ONLYOFFICE Desktop Editors"] border pixel 0#, border normal 0

#for_window [class="Xfce4-panel"] border pixel 0, border normal 0





default_floating_border pixel 0

# внутренние отступы
gaps inner 10

# внешние отступы
gaps outer 2

# Мои комбинации
bindsym $mod+e exec thunar
bindsym $mod+s exec pavucontrol
bindsym $mod+t exec lxtask
bindsym $mod+shift+s exec flameshot gui

# bindsym $mod+o exec openconnect-gui

# setxkbmap -layout "us,ru" -option "grp:win_space_toggle"



exec picom -b --config /home/mrgraf/.config/i3/picom.conf
exec --no-startup-id xfsettingsd 
exec --no-startup-id xfce4-notifyd
exec --no-startup-id thunar --daemon
exec --no-startup-id xset s off -dpms
exec --no-startup-id copyq
exec --no-startup-id yandex-disk-indicator

#bindsym $mod+space lexec --no-startup-id ~/.config/i3/switch-layout.sh




bindsym $mod+shift+p exec picom -b --config /home/mrgraf/.config/i3/picom.conf
bindsym $mod+shift+o exec killall -2 picom

# Блокировка экрана
bindsym $mod+l exec /home/mrgraf/.config/scripts/lock

# Автозапуск
exec_always /home/mrgraf/.config/polybar/launch.sh 
exec --no-startup-id numlockx on
exec --no-startup-id nitrogen --restore; sleep 1 
exec_always --no-startup-id autotiling
#exec --no-startup-id picom -b --config /home/mrgraf/.config/i3/picom
exec --no-startup-id /usr/lib/tumbler-1/tumblerd

# Плавающие окна.
# Что бы определить его класс нужно прописать в терминале "xprop" и тыкнуть на нужную программу, в терминале будет нужная инфа 
for_window [class="Nitrogen" instance="nitrogen"] floating enable
for_window [class="KeePassXC" instance="keepassxc"] floating enable
for_window [class="pavucontrol" instance="pavucontrol"] floating enable

for_window [class="Nvidia-settings" instance="nvidia-settings"] floating enable
for_window [class="Xfce4-panel" instance="xfce4-panel"] floating enable
for_window [class="copyq" instance="copyq"] floating enable


for_window [class="Xfce4-keyboard-settings" instance="xfce4-keyboard-settings"] floating enable
for_window [class="Xfce4-appearance-settings" instance="xfce4-appearance-settings"] floating enable
for_window [class="Xfce4-notifyd-config" instance="xfce4-notifyd-config"] floating enable
for_window [class="Xfce4-mouse-settings" instance="xfce4-mouse-settings"] floating enable

#for_window [class="^.*$"] no_focus -- для всех окон
for_window [class="steam"] no_focus
